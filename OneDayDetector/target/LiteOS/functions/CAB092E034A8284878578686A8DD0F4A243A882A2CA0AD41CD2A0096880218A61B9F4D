static void PrintTensor_64(const void *data, unsigned int size)
{
    long *str = (long *)data;
    for (unsigned int i = 0; i < size - 1; ++i) {
        long c = str[i];
        printf(" %ld,", c);
    }
    printf("\n\n");
    return;
}
