static void PrintTensor_32(const void *data, unsigned int size)
{
    int *str = (int *)data;
    for (unsigned int i = 0; i < size - 1; ++i) {
        int c = str[i];
        printf(" %d,", c);
    }
    printf("\n\n");
    return;
}
