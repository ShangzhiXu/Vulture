UINT64 StmGetTimerCycles(Timer_t num)
{
    UINT64 cycles = 0;

    switch (num) {
        case 7:
            cycles = Timer7Getcycle();
            break;
        default:
            printf("Wrong number of TIMER.\n");
    }
    return cycles;
}
