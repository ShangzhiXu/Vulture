typedef struct {
    UINT16 tranId;
    UINT16 length;
    UINT8  tag;
    UINT8  cmd;
    UINT8  resp;  // value CMD_RESP means need resp, CMD_NO_RESP means need not resp
    UINT8  partialOrder;
} PktHeader;
