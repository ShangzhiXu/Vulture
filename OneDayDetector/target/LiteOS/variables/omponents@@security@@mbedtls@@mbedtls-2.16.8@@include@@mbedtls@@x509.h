    int year, mon, day;         /**< Date. */
    int hour, min, sec;         /**< Time. */
    int hour, min, sec;         /**< Time. */
    int year, mon, day;         /**< Date. */
    int hour, min, sec;         /**< Time. */
    int year, mon, day;         /**< Date. */
